<div class="profile-sidebar d-flex flex-column p-3">
    <!-- User Header -->
    <div class="d-flex align-items-center mb-4 px-2">
        <div class="avatar-circle me-3">
            <img src="{{ asset(app.user.avatarPath) }}" alt="Profile" class="rounded-circle" width="40" height="40">
            <span class="status-indicator"></span>
        </div>
        <div class="user-info">
            <div class="fw-bold small text-truncate" style="max-width: 150px;">{{ app.user.firstname }} {{ app.user.lastname }}</div>
            <div class="text-muted x-small">u/{{ app.user.email|split('@')[0] }}</div>
        </div>
    </div>

    <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
            <a href="{{ path('app_profile') }}" class="nav-link text-white {{ app.request.get('_route') == 'app_profile' ? 'active' : '' }}">
                <i class="bi bi-person me-3"></i> Profile
            </a>
        </li>
        <li>
            <a href="#" class="nav-link text-white">
                <i class="bi bi-person-badge me-3"></i> Change avatar
            </a>
        </li>
        <li>
            <a href="#" class="nav-link text-white">
                <i class="bi bi-journal-text me-3"></i> My Orders
            </a>
        </li>

        <hr class="my-3 border-secondary">

        <li class="nav-section-title px-3 mb-2 text-uppercase text-muted small">Settings</li>
        <li>
            <a href="#" class="nav-link text-white">
                <i class="bi bi-gear me-3"></i> Account settings
            </a>
        </li>
        <li>
            <div class="nav-link text-white d-flex justify-content-between align-items-center">
                <span><i class="bi bi-moon me-3"></i> Display mode</span>
                <div class="form-check form-switch">
                    <input class="form-check-input" type="checkbox" id="darkModeSwitch" >
                </div>
            </div>
        </li>

        <hr class="my-3 border-secondary">

        <li>
            <a href="{{ path('app_logout') }}" class="nav-link text-danger">
                <i class="bi bi-box-arrow-right me-3"></i> Sign out
            </a>
        </li>
    </ul>
</div>